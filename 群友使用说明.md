# we-groupchat-obsidian 群友使用说明

这是一份给群友看的快速说明。实际 Obsidian 输出截图、完整隐私边界、MCP 发送规则、Obsidian 工作流和开发说明请看 `README.zh-CN.md`。

## 先确认

- 只支持 macOS。
- 需要 Python 3.10+、已登录的微信桌面版、Xcode Command Line Tools。
- 需要一个 AI provider API Key，或者本地 Ollama。
- 这不是微信/Tencent 官方软件，不是机器人，也不是远程服务。
- 默认数据目录是 `~/.we-groupchat-obsidian/`，API Key 存在 macOS Keychain。
- 云端 AI 会收到你要求总结的聊天文本；想尽量本地化就用 Ollama。

## 获取源码并第一次运行

在 Terminal 中运行：

```bash
git clone https://github.com/IndelibleVivi/we-groupchat-obsidian.git
cd we-groupchat-obsidian
./启动.command
```

1. 脚本需要创建/更新 `.venv` 并安装 dependencies 时会先询问；输入 `y` 才继续，不同意就退出。
2. 菜单栏出现图标后，进入设置，选择 AI provider 并填写 API Key。
3. 如果 macOS 阻止运行 `.command`，在 Finder 中右键 `启动.command`，选择“打开”，再在弹窗中确认打开。

这是 source-only CLI 分发，不包含 `.dmg` 或 bundled Python runtime。

如果微信更新后需要重新授权，普通启动不会偷偷重签名。确认要继续时再运行：

```bash
./启动.command --allow-wechat-resign
```

这一步可能会退出微信，并要求输入 Mac 登录密码；终端输入密码时不显示字符是正常的。

## 更新

在没有未提交本地改动的 checkout 中运行：

```bash
git pull --ff-only
./启动.command
```

如果 `requirements.txt` 有变化，启动脚本会再次询问是否更新 `.venv` 和 dependencies。

## 常用入口

```bash
./启动.command
./配置关注推送.command
./健康检查.command
./刷新数据源.command
./历史总结到Obsidian.command
./整理Obsidian输出.command
./安装自动启动.command
./卸载自动启动.command
```

## 建议先跑一次健康检查

```bash
./健康检查.command
```

默认输出是 redacted 的，适合排查 DB/key、LaunchAgent、通知 identity 和 Obsidian 输出状态。只有本机私下 debug 才考虑加 `--sensitive`。

## 不要上传或转发这些东西

- `.venv/`
- `.git/`
- `~/.we-groupchat-obsidian/`
- `~/.wechat-summary/`
- `all_keys.json`
- `*.db`
- `*.log`
- API Key、截图里的 token、真实聊天导出、Obsidian 私人 vault 内容

向其他人分享时，直接发送公开 repo 或中文版 README 链接：

- https://github.com/IndelibleVivi/we-groupchat-obsidian
- https://github.com/IndelibleVivi/we-groupchat-obsidian/blob/main/README.zh-CN.md

不要重新压缩自己正在运行的 checkout；其中可能已经出现 `.venv`、本机 runtime、cache、日志或私有调试材料。
